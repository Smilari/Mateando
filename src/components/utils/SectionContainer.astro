---
import SubTitle from "@utils/SubTitle.astro";
const { subtitle, id } = Astro.props;
---

<section id={id} class="container py-20 mx-auto max-w-[92%] sm:max-w-[80%]">
    {subtitle && <SubTitle title={subtitle} />}
    <slot />
</section>